<%= form_for(@session) do |f| %>
  <% if @session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@session.errors.count, "error") %> prohibited this session from being saved:</h2>

      <ul>
      <% @session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :style_id %><br>
    <%= f.number_field :style_id %>
  </div>
  <div class="field">
    <%= f.label :occurs_at %><br>
    <%= f.datetime_select :occurs_at %>
  </div>
  <div class="field">
    <%= f.label :duration %><br>
    <%= f.text_area :duration %>
  </div>
  <div class="field">
    <%= f.label :insructor_id %><br>
    <%= f.number_field :insructor_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
